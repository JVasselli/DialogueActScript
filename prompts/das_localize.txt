# user
    Please localize the following DAS for an [language] speaker while maintaining the original structure and meaning. Do not remove, condense, or add new topics. Only adjust cultural references when necessary, and keep all turns intact. The format must remain exactly the same, with only localized modifications where relevant.

    Target language/culture: [language]
    Summary: [localized_context]

    DAS:
    [turns]

# JSON schema
{
    "name": "local_das",
    "schema": {
        "type": "object",
        "properties": {
            "localized_das": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "turn_id": {"type": "string"},
                        "speaker_id": {"type": "string"},
                        "functions": {"type": "string"}
                    },
                    "required": ["turn_id", "speaker_id", "functions"],
                    "additionalProperties": false
                }
            }
        },
        "required": ["localized_das"],
        "additionalProperties": false
    },
    "strict": true
}